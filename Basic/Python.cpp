//輸入
import sys 
line = sys.stdin.readline() // 會讀到換行
input().strip()

array = [0] * (N) //N個0
range(0, N) // 0 ~ N-1
D, R, N = map(int, line[:-1].split()) // 分三個 int 變數

pow(a, b, c) // a ^ b % c

print(*objects, sep = ' ', end = '\n')
// objects -- 可以一次輸出多個對象
// sep -- 分開多個objects
// end -- 默認值是\n

// EOF break
try:
    while True:
        //input someithing
except EOFError:
    pass