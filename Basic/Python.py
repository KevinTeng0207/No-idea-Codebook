//輸入
input()

array = [0] * (N) //N個0
range(0, N) // 0 ~ N-1
line = input().split()
D, R, N = map(int, line) // 分三個 int 變數

// 才是 取除數
/ 是 小數運算

pow(a, b, c) // a ^ b % c

print(*objects, sep = ' ', end = '\n')
// objects -- 可以一次輸出多個對象
// sep -- 分開多個objects
// end -- 默認值是\n

// EOF break
try:
    while True:
        //input someithing
except EOFError:
    pass